# ── Server ──
app_dir: /opt/trade-engine
repo_url: "https://github.com/thecatblake/kryor-trade-engine.git"
repo_branch: main

# ── Alpaca ──
alpaca_api_key: "PK7FVGGIHL5RUJ6TTIOGJVUNQB"
alpaca_secret_key: "9U3hLn5tcS9SrbZupbND1JQtE7JpyC7znep2KAdY1j4M"
alpaca_paper: "true"

# ── Database ──
postgres_password: "kryor_prod_{{ lookup('password', '/dev/null chars=ascii_lowercase,digits length=12') }}"
questdb_password: "kryor_prod_{{ lookup('password', '/dev/null chars=ascii_lowercase,digits length=12') }}"
grafana_password: "kryor_prod_{{ lookup('password', '/dev/null chars=ascii_lowercase,digits length=12') }}"

# ── Firewall ──
allowed_ssh_ips:
  - "0.0.0.0/0"  # 自分のIPに絞ること
